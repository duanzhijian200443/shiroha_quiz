<USER_REQUEST>
# 【架构师原子任务 38】异步架构升级：全局消息总线与后台静默解析引擎

反重力，用户要求废除阻塞式的导入等待界面，改为“点击即销毁界面，后台静默解析”的现代异步体验。我们需要在 `main.dart` 注入全局 `ScaffoldMessengerKey`，并重构 `import_settings_screen.dart` 将任务剥离出组件生命周期。

⚠️ **【最高执行红线 (Do & Don't)】**：
- **DO (必须做)**：读取 `lib/main.dart` 和 `lib/ui/pages/import_settings_screen.dart`。
- **DO (必须做)**：使用 `write_file` 全量覆写文件，避免局部匹配失败。
- **DON'T (绝对禁止)**：禁止修改底层 `AiService` 的解析逻辑。
- **DON'T (绝对禁止)**：本指令内部全部采用 4 个空格缩进，绝对禁止在回答中使用任何嵌套的三反引号。

请严格按以下 2 步执行：

### 任务 1：注入全局消息总线
读取 `lib/main.dart`。
在文件顶部的全局变量区域，新增 `rootScaffoldMessengerKey`：

    final ValueNotifier<String> globalThemeNotifier = ValueNotifier('light');
    // 核心新增：全局消息总线钥匙，用于跨页面/后台任务弹窗
    final GlobalKey<ScaffoldMessengerState> rootScaffoldMessengerKey = GlobalKey<ScaffoldMessengerState>();

找到 `MaterialApp` 的构建逻辑，将这把钥匙挂载上去：

    return MaterialApp(
      title: 'Shiroha Quiz',
      scaffoldMessengerKey: rootScaffoldMessengerKey, // 挂载全局钥匙
      theme: AppTheme.getTheme(themeName), 
      home: const MainScreen(),
    );

### 任务 2：重构导入页，实现后台静默解析
读取 `lib/ui/pages/import_settings_screen.dart`。
1. 确保顶部导入了 `main.dart` 以获取全局钥匙：
    `import '../../main.dart';`
2. 将 `_ImportSettingsScreenState` 彻底重写，剥离 `PopScope` 和阻塞 Loading，采用 `Future.microtask` 派发后台任务：

    import 'package:flutter/material.dart';
    import 'package:flutter/services.dart';
    import 'package:fil
<truncated 9195 bytes>

                const SizedBox(height: 24),
                SwitchListTile(
                  title: const Text('深度视觉解析 (慢速/极高精度)', style: TextStyle(fontSize: 14, fontWeight: FontWeight.bold)),
                  subtitle: const Text('包含代码截图或复杂公式的 PDF/图片 请开启。', style: TextStyle(fontSize: 12)),
                  value: _useVisionEngine, activeColor: Colors.purpleAccent, contentPadding: EdgeInsets.zero,
                  onChanged: (val) => setState(() => _useVisionEngine = val),
                ),
                const SizedBox(height: 24),
                ElevatedButton.icon(
                  style: ElevatedButton.styleFrom(padding: const EdgeInsets.symmetric(vertical: 16), backgroundColor: theme.primaryColor, foregroundColor: Colors.white, shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12))),
                  icon: const Icon(Icons.upload_file), label: const Text('选择文件并后台解析', style: TextStyle(fontSize: 16, fontWeight: FontWeight.bold)),
                  onPressed: _pickAndParseFile,
                ),
                const SizedBox(height: 12),
                OutlinedButton.icon(
                  style: OutlinedButton.styleFrom(padding: const EdgeInsets.symmetric(vertical: 16), foregroundColor: theme.primaryColor, side: BorderSide(color: theme.primaryColor.withOpacity(0.5)), shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12))),
                  icon: const Icon(Icons.content_paste), label: const Text('从剪贴板后台解析', style: TextStyle(fontSize: 16, fontWeight: FontWeight.bold)),
                  onPressed: _pasteAndParse,
                ),
              ],
            ),
          ),
        );
      }
    }

执行完成后，在终端运行 flutter analyze 确保代码无误。向我报告：“全局消息总线已挂载，后台静默解析架构升级完成。”
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-05-24T13:24:59+08:00.
</ADDITIONAL_METADATA>